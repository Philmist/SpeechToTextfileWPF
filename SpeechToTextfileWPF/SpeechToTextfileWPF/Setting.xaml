<Window x:Class="SpeechToTextfileWPF.SettingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeechToTextfileWPF"
        mc:Ignorable="d"
        Title="SettingDialog" Height="450" Width="800"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:SettingControl />
        <UniformGrid Grid.Row="1" HorizontalAlignment="Right" Rows="1" Columns="3">
            <Button Margin="4" Padding="4" Content="{Binding buttonOK, Source={StaticResource resources}}" />
            <Button Margin="4" Padding="4" Content="{Binding buttonCancel, Source={StaticResource resources}}" />
            <Button Margin="4" Padding="4" Content="{Binding buttonApply, Source={StaticResource resources}}" />
        </UniformGrid>
    </Grid>
</Window>
